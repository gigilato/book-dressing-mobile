import { ViewVariant } from './View.props'

export const viewVariants: Record<ViewVariant, any> = {
  absoluteFill: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    right: 0,
    left: 0,
  },
}
