import { StyleSheet } from 'react-native'
import { theme } from '@theme'

export const styles = StyleSheet.create({
  footerActivityIndicator: {
    paddingVertical: theme.sizes.defaultBottomInset,
  },
})
