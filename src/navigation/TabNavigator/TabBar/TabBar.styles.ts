import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 0,
    left: 32,
    right: 32,
    overflow: 'hidden',
    borderRadius: 24,
  },
  tabBar: {
    height: 70,
    alignItems: 'center',
  },
})
